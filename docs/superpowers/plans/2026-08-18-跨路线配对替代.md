# 跨路线配对替代 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户要求"修改逻辑让结论可用"。路线版（3 条主路径）的达标门槛在同路线配对口下结构性失效（同路线原理趋同→H=1−P_sim 恒 <0.3→达标恒 0）。改为**跨路线配对**：国外路线 Y 替代国内路线 X（X≠Y，共 6 个有序配对），原理差异跨路线天然较大，"达标"判定恢复意义，结论变为"哪条国外路线最可能替代哪条国内路线"（对应文档第四步"A、B 两条路线"的原意）。

**Architecture:** path_routes.py 增建配对表（patent_route_paths.csv 改为 6 配对行，原 3 路线表另存 patent_route_routes.csv）→ indicators.per_topic_indicators 加 exposure 参数（K/A/V 按 B 路线全集算，doc 口径）→ run_all 配对模式（exposure=路线 Y 全集）→ generate_report 路线版摘要/解读重写为配对版。

**Tech Stack:** 既有管道（pandas / indicators / embed_similarity / run_all / generate_report）。

## Global Constraints

- 项目**不是 git 仓库**：无 commit 步骤；工作目录 `D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算`；路径含中文加引号；CSV utf-8-sig；文件头 `# -*- coding: utf-8 -*-`；测试 `python -m pytest tests -v`；禁止硬编码项目内绝对路径。
- IPC 版（147 主题）不改动；路线版中间表：`patent_route_paths.csv`=配对表（6 对）、`patent_route_routes.csv`=3 路线表（供 exposure 与全景）、`mainpath_nodes_by_window_paths.csv` 不变。
- indicators 的既有行为与全部既有测试不破坏（exposure=None 时与现状逐字节等价）。

---

### Task 10: indicators exposure 参数 + path_routes 配对表

**Files:**
- Modify: `替代风险计算/scripts/indicators.py`、`替代风险计算/scripts/path_routes.py`
- Test: `替代风险计算/tests/test_indicators.py`（追加）、`替代风险计算/tests/test_path_routes.py`（追加）

**Interfaces:**
- `per_topic_indicators(..., exposure=None)`：exposure 为专利 dict 列表（含 `is_cn` 键）时，K/A/V 按 exposure 全集计算（num 仍为 for_ 的 q 和；A=exposure 高价值中 is_cn==1 占比）；None 时行为与现状完全一致。
- path_routes.py 新增 `ROUTE_SHORT = {'P1': '推送+测量', 'P2': '数据平台', 'P3': '投放优化'}` 与 `build_pairs_table(routes: pd.DataFrame) -> pd.DataFrame`（6 有序对：dom 行=X 的 CN 专利、for 行=Y 的国外专利；topic_code=`X→Y`；topic_name=`国外Y（简称）替代国内X（简称）`）；`main()` 写 patent_route_paths.csv（配对表）与 patent_route_routes.csv（3 路线表）。

- [ ] **Step 1: 写失败测试**

test_indicators.py 追加：

```python
class TestExposureOverride:
    def test_exposure_drives_k_and_a(self):
        dom = make_patents([('CN1', 2001, 5, {'a'}, {'x'}, {'p1'}),
                            ('CN2', 2002, 5, {'a'}, {'x'}, {'p1'}),
                            ('CN3', 2003, 5, {'a'}, {'x'}, {'p1'})])
        for_ = make_patents([('US1', 2020, 0, {'a'}, {'x'}, {'p2'}),
                             ('US2', 2021, 0, {'a'}, {'x'}, {'p2'}),
                             ('US3', 2022, 0, {'a'}, {'x'}, {'p2'})])
        # exposure = B 路线全集：国外 3 条(in_degree 0) + 国内 2 条(in_degree 2)
        exposure = [dict(p, is_cn=0) for p in for_]
        exposure += [dict(p, is_cn=1) for p in make_patents(
            [('CN9', 2005, 2, {'a'}, {'x'}, {'p1'}),
             ('CN8', 2006, 2, {'a'}, {'x'}, {'p1'})])]
        r = per_topic_indicators('P1→P2', '国外P2替代国内P1', dom, for_,
                                 empty_mainpath(), WINDOW_ENDS, 0,
                                 exposure=exposure)
        # K：num=国外 3 条 q=1 → 3；den=3*1 + 2*3 = 9 → K=1/3
        assert r['K'] == 1 / 3
        # A：阈值 0 → exposure 全部高价值；CN 2/5 = 0.4
        assert r['A'] == 0.4
        assert r['V'] == (1 / 3 + 1 - 0.4) / 2
```

test_path_routes.py 追加：

```python
class TestBuildPairsTable:
    def test_six_pairs(self, tmp_path):
        routes = pd.DataFrame([
            {'pub': 'CNX', 'topic_code': 'P1', 'topic_name': 'x', 'country': '中国',
             'is_cn': 1, 'year': 2010, 'in_degree': 1.0,
             'func': 'a', 'scene': 'x', 'princ': 'p'},
            {'pub': 'USX', 'topic_code': 'P1', 'topic_name': 'x', 'country': '美国',
             'is_cn': 0, 'year': 2011, 'in_degree': 2.0,
             'func': 'b', 'scene': 'y', 'princ': 'q'},
            {'pub': 'USY', 'topic_code': 'P2', 'topic_name': 'y', 'country': '美国',
             'is_cn': 0, 'year': 2012, 'in_degree': 3.0,
             'func': 'c', 'scene': 'z', 'princ': 'r'},
        ])
        pairs = build_pairs_table(routes)
        assert set(pairs['topic_code']) == {'P1→P2', 'P1→P3', 'P2→P1',
                                            'P2→P3', 'P3→P1', 'P3→P2'}
        # P1→P2：dom=P1 的 CN(CNX)、for=P2 的国外(USY)；USX(P1 国外) 不进 P1→P2
        sub = pairs[pairs['topic_code'] == 'P1→P2']
        assert set(sub['pub']) == {'CNX', 'USY'}
        assert sub.iloc[0]['topic_name'] == '国外P2（数据平台）替代国内P1（推送+测量）'
        # 行数 = Σ_{对} (X 的 CN 数 + Y 的国外数) = 2*CNX(2对) + USX(2对) + USY(2对) = 6
        assert len(pairs) == 6
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_indicators.py::TestExposureOverride tests/test_path_routes.py::TestBuildPairsTable -v
```

Expected: FAIL（exposure 参数不存在 / build_pairs_table 不存在）。

- [ ] **Step 3: 实现**

indicators.py 签名加 `exposure=None`，K/A 段替换为：

```python
    all_p = exposure if exposure is not None else dom + for_
    num = sum(p['in_degree'] + 1.0 for p in for_)
    den = sum(p['in_degree'] + 1.0 for p in all_p)
    K = num / den if den > 0 else None

    high = [p for p in all_p if p['in_degree'] >= high_value_threshold]
    if high:
        if exposure is not None:
            A = sum(1 for p in high if p.get('is_cn') == 1) / len(high)
        else:
            A = sum(1 for p in high if p['pub'] in dom_pubs) / len(high)
        V = (K + 1.0 - A) / 2.0
```

docstring 参数行加 `exposure: K/A/V 按此专利列表（含 is_cn）计算；None 时用 dom+for（旧行为）`。

path_routes.py 追加：

```python
ROUTE_SHORT = {'P1': '推送+测量', 'P2': '数据平台', 'P3': '投放优化'}


def build_pairs_table(routes: pd.DataFrame) -> pd.DataFrame:
    """6 个有序路线对 (X→Y, X≠Y)：dom 行 = X 的 CN 专利，for 行 = Y 的国外专利。"""
    cols = ['pub', 'topic_code', 'topic_name', 'country', 'is_cn',
            'year', 'in_degree', 'func', 'scene', 'princ']
    rows = []
    for x in ('P1', 'P2', 'P3'):
        for y in ('P1', 'P2', 'P3'):
            if x == y:
                continue
            code = f'{x}→{y}'
            name = f'国外{y}（{ROUTE_SHORT[y]}）替代国内{x}（{ROUTE_SHORT[x]}）'
            for _, r in routes.loc[routes['topic_code'] == x].iterrows():
                if int(r['is_cn']) == 1:
                    row = dict(r)
                    row['topic_code'], row['topic_name'] = code, name
                    rows.append(row)
            for _, r in routes.loc[routes['topic_code'] == y].iterrows():
                if int(r['is_cn']) == 0:
                    row = dict(r)
                    row['topic_code'], row['topic_name'] = code, name
                    rows.append(row)
    return pd.DataFrame(rows)[cols]
```

main() 改为：

```python
def main() -> None:
    out = build_patent_routes(DS_PATH, PATENT_ROUTE)
    out.to_csv(os.path.join(INTER_DIR, 'patent_route_routes.csv'),
               index=False, encoding='utf-8-sig')
    pairs = build_pairs_table(out)
    pairs.to_csv(os.path.join(INTER_DIR, 'patent_route_paths.csv'),
                 index=False, encoding='utf-8-sig')
    mp = pd.read_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window.csv'), dtype=str)
    mp.to_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window_paths.csv'),
              index=False, encoding='utf-8-sig')
    print(f'patent_route_routes: {len(out)} 行')
    for code, grp in out.groupby('topic_code'):
        print(f'  {code}: {len(grp)} 条（CN {(grp["is_cn"].astype(int) == 1).sum()}）')
    print(f'patent_route_paths（配对表）: {len(pairs)} 行')
```

- [ ] **Step 4: 运行确认通过 + 重生成中间表**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_indicators.py tests/test_path_routes.py -v
```

Expected: 全绿（19+2 + 4+1，以实际为准）。然后：

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/path_routes.py
```

Expected：patent_route_routes 2863 行（P1 966/CN212、P2 866/CN232、P3 1031/CN261）；配对表行数 ≈ 3×CN数+3×国外数（记录真实值）。

---

### Task 11: run_all 配对模式 + 报告配对版 + 全流程验证

**Files:**
- Modify: `替代风险计算/scripts/run_all.py`、`替代风险计算/scripts/generate_report.py`
- Test: `替代风险计算/tests/test_run_all.py`（更新 TestPathsRun/TestPathsEmbedAssembly）、`替代风险计算/tests/test_report.py`（更新 test_paths_summary）

**Interfaces:**
- run_all：suffix=='_paths' 时读 patent_route_paths.csv（配对表）分组；读 patent_route_routes.csv 构造 exposure 字典（route→含 is_cn 的专利 dict 列表）；per_topic_indicators 调用加 `exposure=exposure_map[配对中的 y]`；其余后缀行为不变。
- generate_report：`_paths_summary` 重写为 6 配对版（动态数字+通用措辞）；配对口径 bullet 更新；Top 标题"（本版全部 6 个路线对）"；解读 bullets 更新（删除"达标 0 结构性"bullet，改为动态达标统计 + 跨路线原理解释 + 6 行粒度 caveat）。

- [ ] **Step 1: 更新测试（先写失败）**

test_run_all.py：TestPathsRun 改为：

```python
PAIRS = [f'{x}→{y}' for x in ('P1', 'P2', 'P3') for y in ('P1', 'P2', 'P3') if x != y]


class TestPathsRun:
    def test_paths_outputs_exist(self):
        assert os.path.exists(PATHS_CSV)
        df = pd.read_csv(PATHS_CSV, dtype={'主题码': str})
        assert len(df) == 6
        assert set(df['主题码']) == set(PAIRS)
        assert (df['n_国内'] >= 3).all() and (df['n_国外'] >= 3).all()
        assert (df['风险排名'].notna()).sum() >= 1
        for col in ['S_AB', 'G_AB', 'T_AB', 'M_AB', 'K_B', 'A_B', 'V_B', 'R_AB']:
            vals = df[col].dropna()
            assert ((vals >= 0) & (vals <= 1)).all(), f'{col} 越界'
```

TestPathsEmbedAssembly：`assert len(merged) == 6`（其余不变）。

test_report.py：test_paths_summary 的 fixture 主题码改 `'P1→P2'`、主题名 `'国外P2（数据平台）替代国内P1（推送+测量）'`，n_国内/n_国外 改 212/634；断言 '结论摘要' 保留，'P1（推送+测量）：风险分' 改为 `'国外P2（数据平台）替代国内P1（推送+测量）' in md` 与 `'跨路线' in md`；'技术路线全景' 断言保留。

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_run_all.py::TestPathsRun tests/test_report.py::test_paths_summary -v
```

Expected: FAIL（总表仍 3 行 / 摘要文本不匹配）。

- [ ] **Step 3: 修改 run_all.py（配对模式）**

main() 读中间表段之后加：

```python
    exposure_map = {}
    if suffix == '_paths':
        routes = pd.read_csv(os.path.join(INTER_DIR, 'patent_route_routes.csv'),
                             dtype={'pub': str, 'topic_code': str, 'year': 'Int64'})
        for code in ('P1', 'P2', 'P3'):
            lst = []
            for r in routes.loc[routes['topic_code'] == code].itertuples(index=False):
                lst.append({
                    'pub': r.pub,
                    'year': r.year if pd.notna(r.year) else None,
                    'in_degree': float(r.in_degree),
                    'func': parse_sets(r.func),
                    'scene': parse_sets(r.scene),
                    'princ': parse_sets(r.princ),
                    'is_cn': int(r.is_cn),
                })
            exposure_map[code] = lst
```

per_topic_indicators 调用 kwargs 加：

```python
            exposure=exposure_map.get(code.split('→')[1]) if suffix == '_paths' else None,
```

- [ ] **Step 4: 修改 generate_report.py（配对版文案）**

`_paths_summary` 整体替换为配对版（动态数字）：

```python
def _paths_summary(df: pd.DataFrame) -> str:
    """路线版（6 个跨路线配对）结论摘要：先给结论，再说方法。"""
    ranked = df.loc[df['风险排名'].notna()].sort_values('风险排名')
    top_row = ranked.iloc[0]
    n_qualified = (~df['标记'].astype(str).str.contains(THRESHOLD_FLAG, na=False)).sum()
    k_min, k_max = df['K_B'].min(), df['K_B'].max()
    a_min, a_max = df['A_B'].min(), df['A_B'].max()
    s_min, s_max = df['S_AB'].min(), df['S_AB'].max()
    m_min, m_max = df['M_AB'].min(), df['M_AB'].max()

    lines = ['## 结论摘要（先读这里）\n']
    lines.append(
        f'- **一句话结论**：在国外三条路线替代国内三条路线的全部 6 个配对中，'
        f'**"{top_row["主题名"]}"的风险最高（R={top_row["R_AB"]:.3f}）**；'
        f'其中 {n_qualified} 个配对满足文档"替代候选"三条件（功能/场景足够相似、'
        f'技术原理明显不同）——跨路线配对让"达标"判定恢复意义。\n')
    lines.append(
        '- **我们在算什么**：把"国外技术替代国内技术"拆成三个问题打分（0~1）：'
        '①能不能替代——国外路线 Y 与国内路线 X 做的事像不像（功能、场景越像分越高，'
        '原理差异越大分越高）；②正在发生吗——Y 的专利增长是否快于 X、'
        'Y 在技术主干道（主路径）上的地位是否上升；③被替代了疼不疼——Y 路线'
        '核心技术多少握在国外、中国自己有多少高价值专利。风险 = 能替代 ×'
        '（正在发生 + 疼不疼）÷ 2。\n')
    lines.append('- **三条路线是谁**：P1=根据位置向用户推送刺激内容并用脑电测反应'
                 '（推送+测量）；P2=把测到的脑反应数据收集、存储、分析、可视化'
                 '（数据平台）；P3=根据脑反应决定刺激内容放在哪里、何时放'
                 '（投放优化）。替代配对即"国外某路线替代国内某路线"共 6 对。\n')
    lines.append('- **结果怎么读**（风险从高到低）：')
    for _, r in ranked.iterrows():
        tag = '　← 风险最高' if r['风险排名'] == 1 else ''
        qual = '，达标' if THRESHOLD_FLAG not in str(r['标记']) else ''
        lines.append(f'  - {r["主题名"]}：风险分 {r["R_AB"]:.3f}{qual}{tag}')
    lines.append(
        f'- **三个要点**：①"能不能替代"得分在 {s_min:.2f}~{s_max:.2f}、'
        f'"正在发生"在 {m_min:.2f}~{m_max:.2f}——排序是两者共同作用的结果，'
        '看 Top 表可逐项归因；②真正的警报仍是"国内缺位"——B 路线的核心技术 '
        f'{k_min*100:.0f}%~{k_max*100:.0f}% 被国外专利掌控，中国高价值专利仅占 '
        f'{a_min*100:.1f}%~{a_max*100:.1f}%，风险性质是"国外强势、国内空白"；'
        '③同路线内部的"原理差异"恒低、无法构成替代（旧口径 3 行结果已归档于'
        '过程记录），跨路线配对才能回答"哪条国外路线在替代哪条国内路线"。\n')
    lines.append('- **与细粒度版对照**：按 147 个 IPC 主题计算的细粒度版'
                 '（`替代风险报告.md`）同样指向"国外主导、国内缺位"，两种口径结论一致。\n')
    lines.append('---\n')
    return '\n'.join(lines)
```

配对口径 bullet（caliber=='paths' 分支）改为：

```python
        lines.append('- 配对口径：跨路线替代候选——国内路线 X 与国外路线 Y（X≠Y，共 6 个有序'
                     '配对 X→Y）；3 条路线（P1/P2/P3）由每窗口 15-30 条原生主路径'
                     'LLM 概括合并而来；K/A/V 按 B 路线（Y）全集计算（文档口径）。')
```

Top 标题 paths 版改为 `（本版全部 6 个路线对）`。解读节 paths 三个 bullets 替换为：

```python
    if caliber == 'paths':
        n_qual = (~df['标记'].astype(str).str.contains(THRESHOLD_FLAG, na=False)).sum()
        lines.append(f'- 达标 {n_qual}/6：跨路线配对的"原理差异"天然较高，'
                     '"替代候选"判定有意义；未达标者主要因功能/场景相似度不足。')
        lines.append('- 配对风险的构成看 Top 表：S_AB（能不能替代）与 M_AB（正在发生）'
                     '逐项可归因，V_B 三条 B 路线均接近上限（国外掌控、国内缺位）。')
        lines.append('- 6 个配对上的 S×M 中位数分级粒度粗，仅供示意，不宜作统计结论。')
```

（原三个 paths bullets 删除；"与细粒度版对照" bullet 保留于摘要中，解读节不再重复。）

- [ ] **Step 5: 全流程重跑 + 全量测试 + 报告核对**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/path_routes.py; python -c "import json; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']='_paths'; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"; python scripts/embed_similarity.py; python scripts/run_all.py; python scripts/generate_report.py; python -c "import json; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']=''; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"; python scripts/run_all.py; python scripts/generate_report.py; python -m pytest tests -v
```

Expected：全绿（预计 47 个，以实际为准）；run_all 配对版输出 6 行/排名数/达标数（真实值记录）；原始版回归 147/46/10/44.0。Read `替代风险报告_paths.md` 核对：摘要 6 对排序、达标动态数字、配对口径句、解读 3 bullets、Top 表 6 行。

- [ ] **Step 6: 缓存清理 + 台账/记忆**

缓存清理（中间缓存保留）；台账追加；记忆文件更新配对口径与结论。
