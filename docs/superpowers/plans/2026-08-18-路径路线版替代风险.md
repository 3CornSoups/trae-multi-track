# 路径路线版替代风险 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户拍板"做法一"：以 3 条主路径（每窗口 15-30 条原生路径已概括为 3 条跨窗口路线，含 LLM 差异化特征）作为技术路线，替代 IPC 主题分组，计算替代风险全指标；与 IPC 版并存（后缀 `_paths`）。LLM 相关性过滤（等 key）作为后续叠加层，不在本计划内。

**Architecture:** 新增 path_routes.py（05_合并数据集 → 路线版中间表 patent_route_paths.csv，schema 与 patent_route.csv 相同，topic_code=P1/P2/P3）→ 三脚本加 `input_suffix` 支持（读/写带后缀文件）→ `_paths` 全流程重算 → 报告适配。

**Tech Stack:** Python 3.11、pandas、既有管道（embed_similarity / run_all / generate_report / indicators）。

**Spec:** 本计划即需求；口径沿用已批准设计（公开国家、软阈值、嵌入对称最佳匹配、sigmoid、q=被引+1、top10% 高价值、6 窗口主路径分母=窗口全节点）。

## Global Constraints

- 项目**不是 git 仓库**：无 commit 步骤，保存文件即完成。
- 工作目录：`D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算`。
- 路径含中文，PowerShell 命令中必须加引号；输出 CSV 一律 `encoding='utf-8-sig'`；文件头 `# -*- coding: utf-8 -*-`。
- 测试命令：`python -m pytest tests -v`。
- 禁止硬编码项目内绝对路径（脚本内 `BASE_DIR=dirname(abspath(__file__))`、`ROOT=dirname(BASE_DIR)`）。
- 原始版（IPC 口径）产物不改动；路线版全部带 `_paths` 后缀。
- config.json 新增 `"input_suffix": ""`；跑路线版时临时改为 `"_paths"`，跑完改回 `""`（原始版重跑确认不受影响）。

## 数据事实（2026-08-18 实测，实现时以此为准）

- `数据集合并/outputs/05_合并数据集.csv`：3009 行、2876 唯一 pub；列 period(5 窗口)/path_id(1-3)/类型(原始主路径 179 行+拓展并入 2830)/pub/标题_中文/摘要_中文/申请日/数据来源。
- 3 条路线跨窗口行数：P1=1006、P2=917、P3=1086；每窗口每路线原始主路径节点 9-15 个；15 个 (period,path) 组合的 CN 数全部 ≥3。
- `路径概括与拓展/outputs/period_{窗口}_summary.json`：paths 数组恰 3 条，键为 path_id + 差异化特征（LLM 撰写的中文描述，可作路线名）。
- `替代风险计算/outputs/intermediate/patent_route.csv`：2863 行（KG 有摘要专利），列 pub/topic_code/topic_name/country/is_cn/year/in_degree/func/scene/princ。
- `mainpath_nodes_by_window.csv`：358 行长表（窗口×主路径去重节点），路线版分母沿用原表全节点（不可映射节点计入分母，口径与 IPC 版一致）。

---

### Task 8: path_routes.py → 路线版中间表

**Files:**
- Create: `替代风险计算/scripts/path_routes.py`
- Test: `替代风险计算/tests/test_path_routes.py`

**Interfaces:**
- Produces: `outputs/intermediate/patent_route_paths.csv`（schema 与 patent_route.csv 完全一致：pub, topic_code, topic_name, country, is_cn, year, in_degree, func, scene, princ；topic_code ∈ {P1,P2,P3}，topic_name=路线名）、`outputs/intermediate/mainpath_nodes_by_window_paths.csv`（= 原 mainpath_nodes_by_window.csv 拷贝，分母口径不变）。
- 纯函数：`build_route_names(summary_dir: str) -> dict`（{P1: 名称}：取 2000_2026 summary 该路径的 差异化特征 全文，长度 >60 字截断前 60 字加省略号）；`build_patent_routes(ds_path: str, patent_route_path: str) -> pd.DataFrame`（按 pub 去重归属 path_id：pub 属于多个 path 时取行数最多的 path_id，平局取较小 path_id；join patent_route 补实体/年份/入度；仅保留 join 成功的行）；`main() -> None`（写两个中间表 + 打印统计）。

- [ ] **Step 1: 写失败测试**

`替代风险计算/tests/test_path_routes.py`：

```python
# -*- coding: utf-8 -*-
import os
import sys

import pandas as pd

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from path_routes import build_route_names, build_patent_routes  # noqa: E402

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
INTER = os.path.join(ROOT, 'outputs', 'intermediate')


class TestBuildRouteNames:
    def test_three_routes(self, tmp_path):
        names = build_route_names(str(tmp_path))
        assert set(names) == {'P1', 'P2', 'P3'}
        assert all(names[k] for k in names)  # 非空


class TestBuildPatentRoutes:
    def test_dedup_and_join(self, tmp_path):
        # 构造 ds：P1 行数多；pubX 同时出现在 P1(2 行)与 P2(1 行)→ 归 P1
        ds = pd.DataFrame([
            {'period': '2000_2005', 'path_id': '1', 'pub': 'PUBX'},
            {'period': '2000_2010', 'path_id': '1', 'pub': 'PUBX'},
            {'period': '2000_2015', 'path_id': '2', 'pub': 'PUBX'},
            {'period': '2000_2005', 'path_id': '2', 'pub': 'PUBY'},
            {'period': '2000_2005', 'path_id': '3', 'pub': 'PUBZ'},
            {'period': '2000_2026', 'path_id': '3', 'pub': 'PUBZ'},
        ])
        ds_path = os.path.join(str(tmp_path), 'ds.csv')
        ds.to_csv(ds_path, index=False, encoding='utf-8-sig')
        route = pd.DataFrame([
            {'pub': 'PUBX', 'topic_code': 'A61B5', 'topic_name': 'x',
             'country': '美国', 'is_cn': 0, 'year': 2003, 'in_degree': 5.0,
             'func': 'a|b', 'scene': 'x', 'princ': 'p'},
            {'pub': 'PUBY', 'topic_code': 'A61N1', 'topic_name': 'y',
             'country': '中国', 'is_cn': 1, 'year': 2010, 'in_degree': 1.0,
             'func': 'c', 'scene': 'y', 'princ': 'q'},
            {'pub': 'PUBZ', 'topic_code': '', 'topic_name': '',
             'country': '中国', 'is_cn': 1, 'year': 2022, 'in_degree': 0.0,
             'func': '', 'scene': '', 'princ': ''},
        ])
        route_path = os.path.join(str(tmp_path), 'route.csv')
        route.to_csv(route_path, index=False, encoding='utf-8-sig')
        out = build_patent_routes(ds_path, route_path)
        assert len(out) == 3                       # 三条 pub 都 join 成功
        got = out.set_index('pub')
        assert got.loc['PUBX', 'topic_code'] == 'P1'   # 行数多者优先
        assert got.loc['PUBY', 'topic_code'] == 'P2'
        assert got.loc['PUBZ', 'topic_code'] == 'P3'
        assert got.loc['PUBX', 'is_cn'] == 0       # 原始列保留
        assert got.loc['PUBX', 'func'] == 'a|b'
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_path_routes.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'path_routes'`。

- [ ] **Step 3: 实现 path_routes.py**

```python
# -*- coding: utf-8 -*-
"""路径路线版：3 条主路径作为技术路线 → patent_route_paths.csv（schema 同 patent_route）。"""
import json
import os

import pandas as pd

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(BASE_DIR)
INTER_DIR = os.path.join(ROOT, 'outputs', 'intermediate')
DS_PATH = os.path.join(ROOT, '..', '数据集合并', 'outputs', '05_合并数据集.csv')
SUMMARY_DIR = os.path.join(ROOT, '..', '路径概括与拓展', 'outputs')
PATENT_ROUTE = os.path.join(INTER_DIR, 'patent_route.csv')

WINDOWS = ['2000_2005', '2000_2010', '2000_2015', '2000_2020', '2000_2026']


def build_route_names(summary_dir: str) -> dict:
    """3 条路线名：取 2000_2026 概括的 差异化特征（截 60 字）。"""
    names = {}
    with open(os.path.join(summary_dir, 'period_2000_2026_summary.json'),
              encoding='utf-8') as f:
        d = json.load(f)
    for p in d['paths']:
        pid = int(p['path_id'])
        feat = str(p.get('差异化特征', '') or '').strip()
        label = feat[:60] + ('…' if len(feat) > 60 else '')
        names[f'P{pid}'] = label or f'路线{pid}'
    return names


def build_patent_routes(ds_path: str, patent_route_path: str) -> pd.DataFrame:
    """05 按 pub 去重归属 path_id（行数多者优先，平局取小号），join patent_route。"""
    ds = pd.read_csv(ds_path, dtype=str)
    ds['path_id'] = ds['path_id'].astype(int)
    cnt = ds.groupby(['pub', 'path_id']).size().reset_index(name='n')
    # 行数多者优先；平局取较小 path_id
    best = (cnt.sort_values(['pub', 'n', 'path_id'], ascending=[True, False, True])
            .groupby('pub', as_index=False).first()[['pub', 'path_id']])
    route = pd.read_csv(patent_route_path, dtype={'pub': str, 'topic_code': str})
    out = best.merge(route, on='pub', how='inner')
    out['topic_code'] = 'P' + out['path_id'].astype(str)
    names = build_route_names(SUMMARY_DIR)
    out['topic_name'] = out['topic_code'].map(names)
    cols = ['pub', 'topic_code', 'topic_name', 'country', 'is_cn',
            'year', 'in_degree', 'func', 'scene', 'princ']
    return out[cols]


def main() -> None:
    out = build_patent_routes(DS_PATH, PATENT_ROUTE)
    out.to_csv(os.path.join(INTER_DIR, 'patent_route_paths.csv'),
               index=False, encoding='utf-8-sig')
    # 主路径分母口径不变：原表拷贝
    mp = pd.read_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window.csv'), dtype=str)
    mp.to_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window_paths.csv'),
              index=False, encoding='utf-8-sig')
    print(f'patent_route_paths: {len(out)} 行（研究数据集 2876 中 join 成功数）')
    for code, grp in out.groupby('topic_code'):
        print(f'  {code}: {len(grp)} 条（CN {(grp["is_cn"].astype(int) == 1).sum()}）')


if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 运行确认通过 + 生成路线版中间表**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_path_routes.py -v
```

Expected: PASS，2 个测试全绿。然后：

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/path_routes.py
```

Expected：输出 patent_route_paths 行数（约 2800，= 2876 − 13 无摘要 − 部分未 join）+ 每条路线行数与 CN 数（记录到报告）。

---

### Task 9: input_suffix 基础设施 + 路线版全流程 + 报告适配

**Files:**
- Modify: `替代风险计算/scripts/embed_similarity.py`、`scripts/run_all.py`、`scripts/generate_report.py`、`config.json`
- Test: `替代风险计算/tests/test_run_all.py`（追加路线版 smoke）

**Interfaces:**
- config.json 加 `"input_suffix": ""`；三脚本读该键：中间表与产出文件名插 suffix（`patent_route{suffix}.csv`、`mainpath_nodes_by_window{suffix}.csv`、`entity_sim_by_topic{suffix}.csv`、`替代风险指标总表{suffix}.csv`、`替代风险报告{suffix}.md`）
- 路线版运行：input_suffix 改 `"_paths"` 跑三脚本；跑完改回 `""` 重跑原始版确认无回归

- [ ] **Step 1: 追加路线版 smoke 测试（tests/test_run_all.py 末尾）**

```python
PATHS_CSV = os.path.join(ROOT, 'outputs', '替代风险指标总表_paths.csv')


class TestPathsRun:
    def test_paths_outputs_exist(self):
        assert os.path.exists(PATHS_CSV)
        df = pd.read_csv(PATHS_CSV, dtype={'主题码': str})
        assert len(df) == 3                         # P1/P2/P3
        assert set(df['主题码']) == {'P1', 'P2', 'P3'}
        assert (df['n_国内'] >= 3).all() and (df['n_国外'] >= 3).all()
        assert (df['风险排名'].notna()).sum() >= 1
        for col in ['S_AB', 'G_AB', 'T_AB', 'M_AB', 'K_B', 'A_B', 'V_B', 'R_AB']:
            vals = df[col].dropna()
            assert ((vals >= 0) & (vals <= 1)).all(), f'{col} 越界'
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_run_all.py::TestPathsRun -v
```

Expected: FAIL（文件不存在）。

- [ ] **Step 3: 三脚本加 input_suffix**

`embed_similarity.py` main()：读 config 取 `suffix = cfg.get('input_suffix', '')`；读 `os.path.join(INTER_DIR, f'patent_route{suffix}.csv')`；写 `f'entity_sim_by_topic{suffix}.csv'`；打印带 suffix。`load_entity_vectors` 仍读原始 patent_route.csv（全量实体缓存不变）。

`run_all.py` main()：取 suffix；读三个带 suffix 中间表；写 `f'替代风险指标总表{suffix}.csv'`；打印带 suffix。

`generate_report.py` main()：取 suffix；读 `f'替代风险指标总表{suffix}.csv'`；写 `f'替代风险报告{suffix}.md'`。render_report 不变。

- [ ] **Step 4: config 加 input_suffix 并跑路线版全流程**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -c "import json; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']='_paths'; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"; python scripts/embed_similarity.py; python scripts/run_all.py; python scripts/generate_report.py
```

Expected：三脚本输出带 `_paths` 文件名；run_all 打印 3 行主题/参与排名/达标数（真实结果记录到报告）。然后改回并重跑原始版确认无回归：

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -c "import json; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']=''; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"; python scripts/run_all.py; python scripts/generate_report.py
```

Expected：原始版输出与之前一致（147 主题/46 排名/10 达标）。

- [ ] **Step 5: 全量测试 + 报告核对**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests -v
```

Expected：全绿（原 36 + 2 + 1 = 39 个，以实际为准）。Read `outputs/替代风险报告_paths.md` 核对：Top 表 3 行（含 P1/P2/P3 与路线名）、解读节数字与总表一致、无乱码。

- [ ] **Step 6: 缓存清理 + 台账/记忆**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; Get-ChildItem -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force -Confirm:$false; Get-ChildItem -Recurse -Directory -Filter ".pytest_cache" | Remove-Item -Recurse -Force -Confirm:$false
```

（中间表缓存保留。）台账 progress.md 追加；记忆文件更新（路径路线版产物）。
