# 嵌入相似度升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将替代风险计算的 F/C/H 实体集合相似度从 Jaccard 升级为 bge-small-zh-v1.5 嵌入的对称最佳匹配均值（Jaccard 保留对照列 F_J/C_J/H_J），总表 25→28 列，全流程重跑。

**Architecture:** 新增 embed_similarity.py 产出中间表 entity_sim_by_topic.csv（F_sim/C_sim/P_sim）→ indicators.per_topic_indicators 加可选 fch 覆盖参数（S/达标标记用嵌入值，Jaccard 输出到 F_J/C_J/H_J）→ run_all 传入 fch 并扩列 → generate_report 口径说明更新。

**Tech Stack:** Python 3.11、sentence_transformers 5.6.1、torch 2.13.0+cpu、模型 BAAI/bge-small-zh-v1.5（本地 HF 缓存，无联网）、numpy/pandas/pytest。

**Spec:** `docs/superpowers/specs/2026-08-18-嵌入相似度升级-design.md`

## Global Constraints

- 项目**不是 git 仓库**：无 commit 步骤，保存文件即完成。
- 工作目录：`D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算`。
- 所有路径含中文，PowerShell 命令中必须加引号。
- 输出 CSV 一律 `encoding='utf-8-sig'`；脚本与测试文件头加 `# -*- coding: utf-8 -*-`。
- 测试运行命令：`python -m pytest tests -v`。
- 禁止硬编码绝对路径；脚本内 `BASE_DIR = os.path.dirname(os.path.abspath(__file__))`（=scripts/）、`ROOT = os.path.dirname(BASE_DIR)`（=替代风险计算/）。
- 不改动已交付的 prepare.py；indicators.py 的既有函数与测试断言全部保留。
- 嵌入编码一次性耗时（CPU ~数分钟），必须缓存（npy/npz）支持重跑秒级。

---

### Task 6: embed_similarity.py + indicators.py fch 参数

**Files:**
- Create: `替代风险计算/scripts/embed_similarity.py`
- Modify: `替代风险计算/scripts/indicators.py`（per_topic_indicators 加 fch 参数与 F_J/C_J/H_J 输出）
- Modify: `替代风险计算/config.json`（加 embed_model）
- Test: `替代风险计算/tests/test_embed_similarity.py`（新增）、`替代风险计算/tests/test_indicators.py`（追加）

**Interfaces:**
- Produces:
  - `outputs/intermediate/entity_sim_by_topic.csv`（列 topic_code, F_sim, C_sim, P_sim；P_sim=原理**相似度**，H=1−P_sim 由 run_all 组装）
  - `symmetric_max_match(a_vecs: np.ndarray, b_vecs: np.ndarray) -> float`（归一化向量，值域 [0,1]；空矩阵由调用方处理）
  - `per_topic_indicators(..., fch=None)`：fch 为 `{'F': float, 'C': float, 'H': float}`；传时 S 与 `未达标` 标记用 fch，返回值 F/C/H=fch 值、F_J/C_J/H_J=Jaccard 值；不传时行为与现状完全一致（F_J/C_J/H_J 输出同值）。Task 7 依赖。
- Consumes: `outputs/intermediate/patent_route.csv`（Task 2 产物，列 func/scene/princ 为 `|` 连接实体）、config.json。

- [ ] **Step 1: 写失败测试**

`替代风险计算/tests/test_embed_similarity.py`：

```python
# -*- coding: utf-8 -*-
import os
import sys

import numpy as np
import pandas as pd
import pytest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from embed_similarity import symmetric_max_match  # noqa: E402

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SIM_CSV = os.path.join(ROOT, 'outputs', 'intermediate', 'entity_sim_by_topic.csv')


class TestSymmetricMaxMatch:
    def test_identical(self):
        a = np.array([[1.0, 0.0]])
        assert symmetric_max_match(a, a) == 1.0

    def test_orthogonal(self):
        a = np.array([[1.0, 0.0]])
        b = np.array([[0.0, 1.0]])
        assert symmetric_max_match(a, b) == 0.0

    def test_symmetric(self):
        rng = np.random.default_rng(42)
        a = rng.normal(size=(5, 8))
        b = rng.normal(size=(3, 8))
        a /= np.linalg.norm(a, axis=1, keepdims=True)
        b /= np.linalg.norm(b, axis=1, keepdims=True)
        assert symmetric_max_match(a, b) == symmetric_max_match(b, a)

    def test_two_vs_one(self):
        # 双实体对单实体：每个方向的最佳匹配都是 √2/2
        a = np.array([[1.0, 0.0], [0.0, 1.0]])
        b = np.array([[0.70710678, 0.70710678]])
        assert symmetric_max_match(a, b) == pytest.approx(np.sqrt(2) / 2)


class TestEmbedSmoke:
    def test_sim_table(self):
        df = pd.read_csv(SIM_CSV, dtype={'topic_code': str})
        assert len(df) == 147
        for col in ['F_sim', 'C_sim', 'P_sim']:
            vals = df[col]
            assert ((vals >= 0) & (vals <= 1)).all(), f'{col} 越界'
        assert (df['F_sim'] > 0).sum() > 100  # 嵌入口径下绝大多数主题有功能重叠
```

`替代风险计算/tests/test_indicators.py` 追加：

```python
class TestFchOverride:
    def _fixture(self):
        dom = make_patents([('CN1', 1999, 0, {'a', 'b'}, {'x'}, {'p1'}),
                            ('CN2', 1998, 0, {'b'}, {'x'}, {'p1'}),
                            ('CN3', 1997, 0, {'a'}, {'x'}, {'p1'})])
        for_ = make_patents([('US1', 2022, 0, {'a'}, {'x'}, {'p2'}),
                             ('US2', 2024, 0, {'b'}, {'x'}, {'p2'}),
                             ('US3', 2025, 0, {'a'}, {'x'}, {'p2'})])
        return dom, for_

    def test_fch_drives_s_and_flag(self):
        dom, for_ = self._fixture()
        r = per_topic_indicators('A61B5', '身体诊断测量', dom, for_,
                                 empty_mainpath(), WINDOW_ENDS, 0,
                                 fch={'F': 1.0, 'C': 1.0, 'H': 0.0})
        assert r['F'] == 1.0 and r['C'] == 1.0 and r['H'] == 0.0
        assert r['S'] == 2 / 3  # (1+1+0)/3
        assert '未达标' in r['flags']  # H=0 < 0.3
        # Jaccard 对照值：F_J=2/3（{'a','b'} vs {'a'}）、H_J=1.0（原理零交集）
        assert r['F_J'] == 2 / 3
        assert r['H_J'] == 1.0

    def test_fch_pass_marker(self):
        dom, for_ = self._fixture()
        r = per_topic_indicators('A61B5', '身体诊断测量', dom, for_,
                                 empty_mainpath(), WINDOW_ENDS, 0,
                                 fch={'F': 0.9, 'C': 0.8, 'H': 0.6})
        assert '未达标' not in r['flags']
        assert r['F'] == 0.9 and r['C'] == 0.8 and r['H'] == 0.6
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_embed_similarity.py::TestSymmetricMaxMatch tests/test_indicators.py::TestFchOverride -v
```

Expected: FAIL（`ModuleNotFoundError: No module named 'embed_similarity'` / `TypeError: per_topic_indicators() got an unexpected keyword argument 'fch'`）。

- [ ] **Step 3: 实现 embed_similarity.py**

```python
# -*- coding: utf-8 -*-
"""实体嵌入语义相似度：bge-small-zh-v1.5 编码三类实体集合，
每主题×关系计算对称最佳匹配均值 → entity_sim_by_topic.csv。"""
import json
import os

import numpy as np
import pandas as pd

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(BASE_DIR)
INTER_DIR = os.path.join(ROOT, 'outputs', 'intermediate')
VEC_CACHE = os.path.join(INTER_DIR, 'entity_vecs.npy')   # 纯浮点 npy，无 pickle
NAME_CACHE = os.path.join(INTER_DIR, 'entity_names.csv')  # 实体名一行一个

REL_COLS = {'采用': 'func', '应用于': 'scene', '基于': 'princ'}


def parse_sets(s) -> set:
    if pd.isna(s) or s == '':
        return set()
    return set(str(s).split('|'))


def symmetric_max_match(a_vecs: np.ndarray, b_vecs: np.ndarray) -> float:
    """对称最佳匹配均值：½[mean_a max_b cos + mean_b max_a cos]，值域 [0,1]。

    输入为已归一化的向量矩阵（每行一个实体）；空矩阵由调用方处理。
    """
    cos = a_vecs @ b_vecs.T
    ab = cos.max(axis=1).mean()
    ba = cos.max(axis=0).mean()
    return float((ab + ba) / 2.0)


def load_entity_vectors() -> tuple:
    """编码全部唯一实体（三类并集），npy+names.csv 缓存；返回 (实体名列表, 归一化向量)。"""
    if os.path.exists(VEC_CACHE) and os.path.exists(NAME_CACHE):
        names = (pd.read_csv(NAME_CACHE, header=None, dtype=str)[0]
                   .tolist())
        return names, np.load(VEC_CACHE)

    with open(os.path.join(ROOT, 'config.json'), encoding='utf-8') as f:
        cfg = json.load(f)

    patent = pd.read_csv(os.path.join(INTER_DIR, 'patent_route.csv'), dtype=str)
    entities = set()
    for col in REL_COLS.values():
        for s in patent[col]:
            entities |= parse_sets(s)
    names = sorted(entities)

    from sentence_transformers import SentenceTransformer
    model = SentenceTransformer(cfg['embed_model'])
    vecs = model.encode(names, batch_size=256, show_progress_bar=False,
                        normalize_embeddings=True)
    np.save(VEC_CACHE, vecs)
    pd.Series(names).to_csv(NAME_CACHE, index=False, header=False,
                            encoding='utf-8-sig')
    return names, vecs


def main() -> None:
    patent = pd.read_csv(os.path.join(INTER_DIR, 'patent_route.csv'), dtype=str)
    patent['topic_code'] = patent['topic_code'].fillna('')
    names, vecs = load_entity_vectors()
    idx = {n: i for i, n in enumerate(names)}

    def set_vecs(es: set) -> np.ndarray:
        ids = sorted(idx[n] for n in es)
        return vecs[ids] if ids else np.zeros((0, vecs.shape[1]))

    rows = []
    for code, grp in patent.loc[patent['topic_code'] != ''].groupby('topic_code'):
        dom = grp.loc[grp['is_cn'].astype(int) == 1]
        for_ = grp.loc[grp['is_cn'].astype(int) == 0]
        row = {'topic_code': code}
        for col in REL_COLS.values():
            a = set()
            for s in dom[col]:
                a |= parse_sets(s)
            b = set()
            for s in for_[col]:
                b |= parse_sets(s)
            row[col] = symmetric_max_match(set_vecs(a), set_vecs(b)) if a and b else 0.0
        rows.append(row)

    out = (pd.DataFrame(rows)
           .rename(columns={'func': 'F_sim', 'scene': 'C_sim', 'princ': 'P_sim'}))
    out.to_csv(os.path.join(INTER_DIR, 'entity_sim_by_topic.csv'),
               index=False, encoding='utf-8-sig')
    print(f'entity_sim_by_topic: {len(out)} 行; F_sim>0.6: {(out["F_sim"] > 0.6).sum()}, '
          f'C_sim>0.5: {(out["C_sim"] > 0.5).sum()}, P_sim>0.7: {(out["P_sim"] > 0.7).sum()}')


if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 修改 indicators.py（fch 参数）**

per_topic_indicators 签名加 `fch=None`；S 计算段替换为：

```python
    F_j = jaccard(entity_union(dom, 'func'), entity_union(for_, 'func'))
    C_j = jaccard(entity_union(dom, 'scene'), entity_union(for_, 'scene'))
    H_j = 1.0 - jaccard(entity_union(dom, 'princ'), entity_union(for_, 'princ'))

    if fch is not None:
        F, C, H = float(fch['F']), float(fch['C']), float(fch['H'])
    else:
        F, C, H = F_j, C_j, H_j

    S = float(w[0] * F + w[1] * C + w[2] * H)
    if not (F >= thresholds['F'] and C >= thresholds['C'] and H >= thresholds['H']):
        flags.append(THRESHOLD_FLAG)
```

返回 dict 加 `'F_J': F_j, 'C_J': C_j, 'H_J': H_j,`。原 `F = jaccard(...)` 等三行删除（由上面替代）；docstring 参数行加 `fch: {'F','C','H'} 嵌入相似度覆盖值（None 时用 Jaccard，即旧行为）`。

- [ ] **Step 5: config.json 加 embed_model**

```json
  "embed_model": "BAAI/bge-small-zh-v1.5"
```

- [ ] **Step 6: 运行确认通过 + 生成中间表**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_embed_similarity.py::TestSymmetricMaxMatch tests/test_indicators.py -v
```

Expected: TestSymmetricMaxMatch 4/4 + TestFchOverride 2/2 + 原 18 个全绿。

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/embed_similarity.py
```

Expected：输出 `entity_sim_by_topic: 147 行; F_sim>0.6: N, C_sim>0.5: M, P_sim>0.7: K`（N/M/K 为真实数据值，记录到报告）。

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_embed_similarity.py -v
```

Expected: 5/5 全绿（smoke 断言 `(F_sim > 0).sum() > 100` 应通过；若实际 <100，停下核对嵌入质量并报告）。

---

### Task 7: run_all / generate_report 集成 + 全流程验证

**Files:**
- Modify: `替代风险计算/scripts/run_all.py`、`替代风险计算/scripts/generate_report.py`
- Test: `替代风险计算/tests/test_run_all.py`（更新）
- 全流程重跑 + 缓存清理 + 台账更新

**Interfaces:**
- Consumes: `outputs/intermediate/entity_sim_by_topic.csv`（Task 6 产物）、`per_topic_indicators(..., fch=...)`。
- Produces: 总表 28 列（S_AB 后插入 F_J/C_J/H_J）；报告口径更新。

- [ ] **Step 1: 更新 test_run_all.py**

`test_output_csv_exists_and_sane` 的数值域循环列改为：

```python
        for col in ['S_AB', 'G_AB', 'T_AB', 'M_AB', 'K_B', 'A_B', 'V_B', 'R_AB',
                    'F_J', 'C_J', 'H_J']:
```

并追加行为断言（嵌入口径应显著高于字符串口径）：

```python
        # 嵌入口径 F 平均应高于 Jaccard 对照（同义异形被捕捉）
        assert df['F_AB'].dropna().mean() > df['F_J'].dropna().mean()
        assert df['F_AB'].dropna().mean() > 0.3
```

`test_ranking_consistent` 不变；TestThresholdSource 两个测试不变。

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_run_all.py -v
```

Expected: FAIL（旧 CSV 无 F_J 列 → KeyError；或数值断言失败）。

- [ ] **Step 3: 修改 run_all.py**

1. `COLUMN_MAP` 加 `'F_J': 'F_J', 'C_J': 'C_J', 'H_J': 'H_J'`，`COLUMN_ORDER` 在 `'S_AB'` 之后插入 `'F_J', 'C_J', 'H_J'`。
2. main() 中读中间表后加：

```python
    sim = pd.read_csv(os.path.join(INTER_DIR, 'entity_sim_by_topic.csv'),
                      dtype={'topic_code': str})
    sim_map = {r.topic_code: r for r in sim.itertuples(index=False)}
```

3. 循环内调用 per_topic_indicators 加参数（在 `min_patents=cfg['min_patents'],` 之后）：

```python
            fch={'F': float(sim_map[code].F_sim),
                 'C': float(sim_map[code].C_sim),
                 'H': 1.0 - float(sim_map[code].P_sim)},
```

- [ ] **Step 4: 修改 generate_report.py**

方法段第二行替换为：

```python
    lines.append('- 可替代性：S_AB = w1·F_AB + w2·C_AB + w3·H_AB，F=功能相似度、C=场景相似度、'
                 'H=原理差异度；F/C 与原理相似度 P 采用 bge-small-zh-v1.5 嵌入的对称最佳匹配'
                 '余弦（H=1−P）；F_J/C_J/H_J 为 Jaccard 字符串口径对照列。')
```

解读节第二行替换为（动态统计）：

```python
    f_j_pos = int((df['F_J'] > 0).sum())
    lines.append(f'- 嵌入口径下 F_AB 均值 {df["F_AB"].dropna().mean():.3f}、'
                 f'C_AB 均值 {df["C_AB"].dropna().mean():.3f}；Jaccard 对照 F_J>0 仅 '
                 f'{f_j_pos} 个主题——字符串相等对同义异形（"RF信号传输能量" vs "RF能量传输"）'
                 '零容错，两种口径的差异为方法学发现。')
```

局限节第 4 条替换为：

```python
    lines.append('4. Jaccard 对照列（F_J/C_J/H_J）显著低于嵌入口径：字符串口径对同义异形'
                 '零容错，导致 92% 主题零交集；嵌入对称最佳匹配对长尾噪声短语较宽容，'
                 '个别主题的相似度可能被少数通用词（如"信号处理"）抬高。')
```

- [ ] **Step 5: 全流程重跑验证**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/prepare.py; python scripts/embed_similarity.py; python scripts/run_all.py; python scripts/generate_report.py; python -m pytest tests -v
```

Expected：全部无报错；pytest 全绿（18+5+5+4+1+2=35 个测试，以实际为准）；run_all 输出主题数 147、达标主题数 > 0（嵌入口径下应有达标，若为 0 停下核对并报告）。

- [ ] **Step 6: 报告核对**

Read `outputs/替代风险报告.md`：方法段含嵌入表述与对照列说明、解读节两口径对比数字与总表一致、Top-20 数值与 CSV 一致、无乱码。抽查 `outputs/替代风险指标总表.csv` 28 列头与 F_AB>F_J 的均值关系。

- [ ] **Step 7: 缓存清理 + 台账**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; Get-ChildItem -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force -Confirm:$false; Get-ChildItem -Recurse -Directory -Filter ".pytest_cache" | Remove-Item -Recurse -Force -Confirm:$false
```

（entity_vecs.npz 与 entity_sim_by_topic.csv 是中间表缓存，**保留**。）台账 `progress.md` 追加本次升级结果行；更新记忆文件 `trae-patent-citation-network.md` 相似度口径描述。
