# BCI 相关性过滤重算 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对研究数据集+主路径节点做 LLM"是否属脑机接口领域"判定，滤除无关专利后在过滤域上重算替代风险全指标（原始版保留，过滤版带 `_filtered` 后缀）。

**Architecture:** 新增 filter_relevance.py（任务构造→调用 batch-glm-llm skill 的 batch_llm.py→解析→过滤中间表）→ embed_similarity/run_all/generate_report 加 `input_suffix` 支持 → 过滤版全流程重跑 → 报告新增「研究域界定」节。

**Tech Stack:** Python 3.11、DeepSeek API（deepseek-v4-flash，`--provider deepseek` 直连，key 走 DEEPSEEK_API_KEY 环境变量）、batch-glm-llm skill（`C:\Users\26610\.claude\skills\batch-glm-llm\scripts\batch_llm.py`）、既有嵌入/指标管道。

**Spec:** `docs/superpowers/specs/2026-08-18-BCI相关性过滤重算-design.md`

## Global Constraints

- 项目**不是 git 仓库**：无 commit 步骤，保存文件即完成。
- 工作目录：`D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算`。
- 路径含中文，PowerShell 命令中必须加引号；输出 CSV 一律 `encoding='utf-8-sig'`；文件头 `# -*- coding: utf-8 -*-`。
- 测试命令：`python -m pytest tests -v`。
- **API key 绝不写入任何文件/命令输出**——只从环境变量 DEEPSEEK_API_KEY 读取（用户已 setx 到用户环境）。
- 禁止硬编码项目内绝对路径（skill 工具路径 BATCH_LLM 是外部依赖，允许常量+注释）。
- 原始版产物（patent_route.csv、总表、报告）不改动；过滤版全部带 `_filtered` 后缀。
- LLM 判定约 3000 条，DeepSeek 直连并发 10 约 10-15 分钟，属正常。

---

### Task 8: filter_relevance.py + LLM 判定

**Files:**
- Create: `替代风险计算/scripts/filter_relevance.py`
- Test: `替代风险计算/tests/test_filter_relevance.py`

**Interfaces:**
- 中间产物（`outputs/intermediate/`）：`relevance/tasks.jsonl`、`relevance/results.jsonl`（batch_llm 输出）、`patent_route_filtered.csv`、`mainpath_nodes_by_window_filtered.csv`、`relevance_filter_stats.csv`、`excluded_patents.csv`
- 纯函数：`build_tasks(df: pd.DataFrame, out_path: str) -> int`（df 列 pub/title/abstract）；`parse_results(path: str) -> pd.DataFrame`（列 pub/relevant/category/reason）；`apply_filter(patent, mainpath, rel, out_dir) -> dict`（返回统计）。Task 9 依赖中间产物文件名。
- Consumes: `05_合并数据集.csv`、`03_合并专利信息_精简.csv`、`patent_route.csv`、`mainpath_nodes_by_window.csv`。

- [ ] **Step 1: 写失败测试**

`替代风险计算/tests/test_filter_relevance.py`：

```python
# -*- coding: utf-8 -*-
import json
import os
import sys

import pandas as pd
import pytest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from filter_relevance import build_tasks, parse_results, apply_filter, normalize_pub  # noqa: E402

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
INTER = os.path.join(ROOT, 'outputs', 'intermediate')


class TestBuildTasks:
    def test_task_shape(self, tmp_path):
        df = pd.DataFrame([
            {'pub': 'US20040102828A1', 'title': '颅内电极方法',
             'abstract': '本发明涉及颅内电极和用于植入颅内电极的方法'},
        ])
        out = os.path.join(str(tmp_path), 'tasks.jsonl')
        n = build_tasks(df, out)
        assert n == 1
        with open(out, encoding='utf-8') as f:
            task = json.loads(f.readline())
        assert task['id'] == 'US20040102828A1'
        assert task['messages'][0]['role'] == 'system'
        assert '脑机接口' in task['messages'][0]['content']
        assert task['messages'][1]['role'] == 'user'
        assert '颅内电极' in task['messages'][1]['content']

    def test_empty_input(self, tmp_path):
        out = os.path.join(str(tmp_path), 'tasks.jsonl')
        assert build_tasks(pd.DataFrame(columns=['pub', 'title', 'abstract']), out) == 0


class TestParseResults:
    def test_normal_json(self, tmp_path):
        p = os.path.join(str(tmp_path), 'r.jsonl')
        with open(p, 'w', encoding='utf-8') as f:
            f.write(json.dumps({'id': 'P1', 'ok': True,
                                'response': '{"relevant": true, "category": "核心", "reason": "BCI植入电极"}',
                                'provider': 'deepseek'}, ensure_ascii=False) + '\n')
            f.write(json.dumps({'id': 'P2', 'ok': True,
                                'response': '```json\n{"relevant": false, "category": "无关", "reason": "位置广告"}\n```',
                                'provider': 'deepseek'}, ensure_ascii=False) + '\n')
            f.write(json.dumps({'id': 'P3', 'ok': False, 'error': 'HTTP 429'}, ensure_ascii=False) + '\n')
        df = parse_results(p)
        assert len(df) == 2                       # 失败的 P3 不入判定表
        assert df.loc[df['pub'] == 'P1', 'relevant'].iloc[0] == True
        assert df.loc[df['pub'] == 'P2', 'relevant'].iloc[0] == False

    def test_malformed_response_tolerated(self, tmp_path):
        p = os.path.join(str(tmp_path), 'r.jsonl')
        with open(p, 'w', encoding='utf-8') as f:
            f.write(json.dumps({'id': 'P1', 'ok': True,
                                'response': '这不是JSON',
                                'provider': 'deepseek'}, ensure_ascii=False) + '\n')
        df = parse_results(p)
        assert len(df) == 0                       # 无法解析 → 丢弃（保守：不默认纳入）


class TestApplyFilter:
    def test_filter_basic(self, tmp_path):
        patent = pd.DataFrame([
            {'pub': 'P1', 'topic_code': 'A61B5', 'is_cn': '1'},
            {'pub': 'P2', 'topic_code': 'A61B5', 'is_cn': '0'},
        ])
        mainpath = pd.DataFrame([
            {'window': 'pre2000', 'pub': 'P1'},
            {'window': 'pre2000', 'pub': 'P3'},   # 无文本未判定 → 默认排除
        ])
        rel = pd.DataFrame([
            {'pub': 'P1', 'relevant': True, 'category': '核心', 'reason': 'BCI'},
            {'pub': 'P2', 'relevant': False, 'category': '无关', 'reason': '广告'},
        ])
        stats = apply_filter(patent, mainpath, rel, str(tmp_path))
        assert stats['patent_kept'] == 1 and stats['patent_dropped'] == 1
        assert stats['mainpath_kept'] == 1 and stats['mainpath_dropped'] == 1
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_filter_relevance.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'filter_relevance'`。

- [ ] **Step 3: 实现 filter_relevance.py**

```python
# -*- coding: utf-8 -*-
"""BCI 相关性过滤：构造 LLM 判定任务 → 调 batch_llm.py → 解析 → 过滤中间表。

判定口径（用户 2026-08-18 拍板）：含 BCI 边缘应用——涉及脑信号采集/解码/
神经调控技术，或直接消费脑信号的系统（神经营销、BCI 教育、神经反馈媒体）为
relevant；仅通用广告/一般医疗/纯算法工具等为 irrelevant。
"""
import json
import os
import re
import subprocess
import sys

import pandas as pd

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(BASE_DIR)
INTER_DIR = os.path.join(ROOT, 'outputs', 'intermediate')
WORK_DIR = os.path.join(INTER_DIR, 'relevance')
# 外部工具（batch-glm-llm 用户级 skill，项目外依赖）
BATCH_LLM = r'C:\Users\26610\.claude\skills\batch-glm-llm\scripts\batch_llm.py'

DS_PATH = os.path.join(ROOT, '..', '数据集合并', 'outputs', '05_合并数据集.csv')
META_PATH = os.path.join(ROOT, '..', '专利数据合并与引文网络构建',
                         '03_合并专利信息_精简.csv')

SYSTEM_PROMPT = (
    '你是专利技术领域判定专家。判断专利是否属于脑机接口（BCI）领域或其直接应用。\n'
    '判定标准：\n'
    '- 属于（relevant=true）：技术涉及脑信号的采集、解码、处理或神经调控'
    '（电/磁/光/超声刺激），或系统直接消费脑信号/神经状态——含神经营销（测脑电反应的）、'
    'BCI 教育训练、神经反馈媒体、BCI 康复/义肢/机器人控制等边缘应用。\n'
    '- 不属于（relevant=false）：与脑信号无关（如仅基于位置或一般用户数据的广告、'
    '不涉及脑信号的一般医疗器械、纯通用算法工具）；脑信号仅为众多生理信号之一'
    '且非系统核心功能。\n'
    '只输出一个 JSON 对象，格式：'
    '{"relevant": true/false, "category": "核心|边缘应用|无关", "reason": "不超过30字"}，'
    '不要输出任何其他内容。')


def normalize_pub(pub: str) -> str:
    return str(pub).replace('-', '').strip().upper()


def collect_texts() -> pd.DataFrame:
    """研究数据集 ∪ 主路径节点 → 每专利一行 [pub, title, abstract]（无文本的保留空值）。"""
    ds = pd.read_csv(DS_PATH, dtype=str)
    ds['pub'] = ds['pub'].map(normalize_pub)
    texts = (ds.drop_duplicates('pub')[['pub', '标题_中文', '摘要_中文']]
             .rename(columns={'标题_中文': 'title', '摘要_中文': 'abstract'}))

    mp = pd.read_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window.csv'), dtype=str)
    mp_pubs = set(mp['pub'].map(normalize_pub))
    extra = mp_pubs - set(texts['pub'])
    if extra:
        meta = pd.read_csv(META_PATH, dtype=str)
        meta['公开号'] = meta['公开号'].map(normalize_pub)
        back = (meta[meta['公开号'].isin(extra)]
                .rename(columns={'公开号': 'pub', '专利标题（中文）': 'title',
                                 '摘要_中文': 'abstract'})[['pub', 'title', 'abstract']])
        texts = pd.concat([texts, back], ignore_index=True)
    # 主路径中无文本节点（无法判定 → 默认排除）单独补行
    missing = sorted(mp_pubs - set(texts['pub']))
    if missing:
        texts = pd.concat([texts, pd.DataFrame(
            [{'pub': p, 'title': '', 'abstract': ''} for p in missing])],
            ignore_index=True)
    return texts


def build_tasks(df: pd.DataFrame, out_path: str) -> int:
    """df 列 [pub, title, abstract] → tasks.jsonl（无文本的行跳过）。"""
    os.makedirs(os.path.dirname(out_path), exist_ok=True)
    n = 0
    with open(out_path, 'w', encoding='utf-8') as f:
        for _, r in df.iterrows():
            if not str(r['title']).strip() and not str(r['abstract']).strip():
                continue
            user = (f'专利公开号：{r["pub"]}\n标题：{r["title"]}\n'
                    f'摘要：{r["abstract"]}\n请判定该专利是否属于脑机接口领域。')
            f.write(json.dumps(
                {'id': r['pub'],
                 'messages': [{'role': 'system', 'content': SYSTEM_PROMPT},
                              {'role': 'user', 'content': user}]},
                ensure_ascii=False) + '\n')
            n += 1
    return n


def run_batch(tasks_path: str, results_path: str) -> None:
    """调用 batch_llm.py（DeepSeek 直连，key 从环境变量读）。"""
    cmd = [sys.executable, BATCH_LLM, '--input', tasks_path,
           '--output', results_path, '--provider', 'deepseek',
           '--concurrency', '10', '--max-tokens', '300',
           '--temperature', '0.2', '--retries', '3', '--resume']
    subprocess.run(cmd, check=True)


def _extract_json(text: str) -> dict | None:
    if not text:
        return None
    text = text.strip()
    m = re.search(r'\{[^{}]*\}', text)
    if m:
        try:
            return json.loads(m.group(0))
        except json.JSONDecodeError:
            return None
    return None


def parse_results(path: str) -> pd.DataFrame:
    """batch_llm 输出 → [pub, relevant, category, reason]（失败/无法解析行丢弃）。"""
    rows = []
    with open(path, encoding='utf-8') as f:
        for line in f:
            if not line.strip():
                continue
            try:
                rec = json.loads(line)
            except json.JSONDecodeError:
                continue
            if not rec.get('ok'):
                continue
            parsed = _extract_json(rec.get('response', ''))
            if parsed is None or 'relevant' not in parsed:
                continue
            rows.append({
                'pub': normalize_pub(rec['id']),
                'relevant': bool(parsed['relevant']),
                'category': str(parsed.get('category', '')),
                'reason': str(parsed.get('reason', '')),
            })
    return pd.DataFrame(rows, columns=['pub', 'relevant', 'category', 'reason'])


def apply_filter(patent: pd.DataFrame, mainpath: pd.DataFrame,
                 rel: pd.DataFrame, out_dir: str) -> dict:
    """按判定结果过滤两中间表 → *_filtered.csv + 统计/排除清单。"""
    os.makedirs(out_dir, exist_ok=True)
    keep = set(rel.loc[rel['relevant'], 'pub'])

    p_f = patent[patent['pub'].isin(keep)]
    p_f.to_csv(os.path.join(out_dir, 'patent_route_filtered.csv'),
               index=False, encoding='utf-8-sig')
    m_f = mainpath[mainpath['pub'].isin(keep)]
    m_f.to_csv(os.path.join(out_dir, 'mainpath_nodes_by_window_filtered.csv'),
               index=False, encoding='utf-8-sig')

    dropped = patent[~patent['pub'].isin(keep)]
    excluded = dropped.merge(rel[rel['relevant'] == False],
                             on='pub', how='left')
    excluded.to_csv(os.path.join(out_dir, 'excluded_patents.csv'),
                    index=False, encoding='utf-8-sig')

    stats = {
        'patent_total': len(patent), 'patent_kept': len(p_f),
        'patent_dropped': len(dropped),
        'mainpath_total': len(mainpath), 'mainpath_kept': len(m_f),
        'mainpath_dropped': len(mainpath) - len(m_f),
        'topics_total': patent['topic_code'].nunique(),
        'topics_kept': p_f['topic_code'].nunique(),
        'cn_kept': int((p_f['is_cn'].astype(int) == 1).sum()),
    }
    pd.DataFrame([stats]).to_csv(os.path.join(out_dir, 'relevance_filter_stats.csv'),
                                 index=False, encoding='utf-8-sig')
    return stats


def main() -> None:
    os.makedirs(WORK_DIR, exist_ok=True)
    texts = collect_texts()
    tasks_path = os.path.join(WORK_DIR, 'tasks.jsonl')
    results_path = os.path.join(WORK_DIR, 'results.jsonl')
    n = build_tasks(texts, tasks_path)
    print(f'tasks: {n}（无文本跳过 {len(texts) - n}）')
    run_batch(tasks_path, results_path)
    rel = parse_results(results_path)
    print(f'results 解析: {len(rel)} 条（relevant {rel["relevant"].sum()} / '
          f'irrelevant {(~rel["relevant"]).sum()}）')

    patent = pd.read_csv(os.path.join(INTER_DIR, 'patent_route.csv'),
                         dtype={'pub': str, 'topic_code': str})
    mainpath = pd.read_csv(os.path.join(INTER_DIR, 'mainpath_nodes_by_window.csv'),
                           dtype=str)
    stats = apply_filter(patent, mainpath, rel, INTER_DIR)
    print('过滤统计:', stats)


if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 运行确认通过 + 真实判定**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_filter_relevance.py -v
```

Expected: PASS，5 个测试全绿。

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/filter_relevance.py
```

Expected：输出 tasks 数（约 3000）、results 解析数与过滤统计；DeepSeek 直连约 10-15 分钟。**若失败任务 >5%，重跑（--resume 已内建）后记录。** 把过滤统计（kept/dropped、主题数、CN 数）与 relevant 分布（核心/边缘/无关）记录到报告。

---

### Task 9: suffix 管道 + 过滤版全流程重算 + 报告

**Files:**
- Modify: `替代风险计算/scripts/embed_similarity.py`、`scripts/run_all.py`、`scripts/generate_report.py`、`config.json`
- Test: `替代风险计算/tests/test_run_all.py`（追加 filtered smoke）

**Interfaces:**
- config.json 加 `"input_suffix": ""`；三个脚本读该键：中间表/产出文件名插 suffix（`patent_route{suffix}.csv`、`mainpath_nodes_by_window{suffix}.csv`、`entity_sim_by_topic{suffix}.csv`、`替代风险指标总表{suffix}.csv`、`替代风险报告{suffix}.md`）
- 过滤版运行：config 的 input_suffix 改为 `"_filtered"` 重跑三脚本（原始版产物不动，需先跑完原始版再切换）

- [ ] **Step 1: 追加 filtered smoke 测试（tests/test_run_all.py 末尾）**

```python
FILTERED_CSV = os.path.join(ROOT, 'outputs', '替代风险指标总表_filtered.csv')


class TestFilteredRun:
    def test_filtered_outputs_exist(self):
        assert os.path.exists(FILTERED_CSV)
        df = pd.read_csv(FILTERED_CSV, dtype={'主题码': str})
        assert len(df) == 147                       # 主题全集仍在（含空主题）
        assert (df['n_国内'] + df['n_国外']).sum() < 2863  # 过滤后专利数少于全量
        assert (df['风险排名'].notna()).sum() >= 1
        for col in ['S_AB', 'R_AB']:
            vals = df[col].dropna()
            assert ((vals >= 0) & (vals <= 1)).all(), f'{col} 越界'
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests/test_run_all.py::TestFilteredRun -v
```

Expected: FAIL（filtered 文件不存在）。

- [ ] **Step 3: 三脚本加 suffix 支持**

`embed_similarity.py`：main() 读 config 取 `suffix = cfg.get('input_suffix', '')`；读 `patent_route{suffix}.csv`；写 `entity_sim_by_topic{suffix}.csv`。`load_entity_vectors` 读原始 patent_route.csv（全量实体缓存不变）。

`run_all.py`：main() 取 suffix；读 `patent_route{suffix}.csv`、`mainpath_nodes_by_window{suffix}.csv`、`entity_sim_by_topic{suffix}.csv`；写 `替代风险指标总表{suffix}.csv`；打印带 suffix。

`generate_report.py`：main() 取 suffix；读 `替代风险指标总表{suffix}.csv`；写 `替代风险报告{suffix}.md`；新增「研究域界定」节（读 `relevance_filter_stats.csv` 与 `excluded_patents.csv` 动态生成：判定方法/边界/过滤前后统计/被排除主题 Top 清单）——render_report 加可选参数 `domain_stats: dict|None = None` 与 `excluded_top: pd.DataFrame|None = None`（测试传 None 走旧路径）。

- [ ] **Step 4: config 加 input_suffix 并跑过滤版全流程**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -c "import json,io; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']='_filtered'; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"
```

然后：

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python scripts/embed_similarity.py; python scripts/run_all.py; python scripts/generate_report.py
```

Expected：三个脚本输出带 `_filtered` 的文件名；run_all 打印过滤版主题数/参与排名/达标数（数值为真实结果，记录到报告）。再把 config 的 input_suffix 改回 `""` 并重跑原始版确认未受影响：

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -c "import json; p='config.json'; c=json.load(open(p,encoding='utf-8')); c['input_suffix']=''; json.dump(c,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)"; python scripts/run_all.py; python scripts/generate_report.py
```

- [ ] **Step 5: 全量测试 + 报告核对**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; python -m pytest tests -v
```

Expected：全绿（原 36 + 5 + 1 = 42 个，以实际为准）。Read `outputs/替代风险报告_filtered.md` 核对：研究域界定节数字与 stats CSV 一致、Top-20 与总表一致、无乱码。

- [ ] **Step 6: 缓存清理 + 台账/记忆**

```powershell
cd "D:\论文和代码项目\论文\TRAE\多轨道\替代风险计算"; Get-ChildItem -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force -Confirm:$false; Get-ChildItem -Recurse -Directory -Filter ".pytest_cache" | Remove-Item -Recurse -Force -Confirm:$false
```

（中间表缓存与 results.jsonl 保留。）台账 progress.md 追加；记忆文件更新（相关性过滤口径与过滤版产物位置）。
