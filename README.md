# TRAE 多轨道专利技术替代风险研究

基于 **专利引文网络 + 主路径识别 + 知识图谱** 的脑机接口（BCI）领域技术替代风险量化研究。

## 快速入口

| 想了解什么 | 看这里 |
|---|---|
| 风险识别实验的逻辑与口径 | **[替代风险计算/README.md](替代风险计算/README.md)**（三步拆解、口径决策、复现方法） |
| 全部结论与解读 | **[替代风险计算/outputs/风险分析报告.md](替代风险计算/outputs/风险分析报告.md)**（四版本对比 + 三个关键发现） |
| 全部指标数据 | `替代风险计算/outputs/替代风险指标总表*.csv`（四版本 × 28 列） |
| 技术路线演化证据 | `替代风险计算/outputs/主路径全景概况.md`（163 条主路径技术主线） |
| 方法与设计文档 | `docs/superpowers/specs/`（研究设计、口径决策记录） |

## 一句话结论

脑机接口领域风险靠前的技术路线全部呈现**"国外主导、国内缺位"**格局（核心技术 95%+ 在国外、中国高价值专利仅 1~3%）：细粒度排查中风险最高的是 **G06Q30 神经营销**（R=0.463，过滤后）；宏观配对中风险最高的是**"国外 P1（推送+测量）替代国内 P2（数据平台）"**（R=0.442），唯一达标候选为**"国外 P2（数据平台）替代国内 P1（推送+测量）"**（R=0.428）。两种划分方式、过滤前后，结论一致。

## 研究流程

```
原始专利数据 → 引文网络构建 → 主路径识别（6 时间窗口） → 语义相似度拓展研究数据集
→ LLM 实体抽取与知识图谱 → BCI 相关性过滤（LLM 判定 2969 条）
→ 技术替代风险计算（双口径 × 过滤前后四版本）
```

## 目录结构

| 目录 | 内容 |
|---|---|
| `替代风险计算/` | 风险识别实验：README（逻辑）+ 8 脚本 + 59 测试 + 四版总表与合并报告 |
| `主路径识别/` | 6 窗口主路径识别结果与报告 |
| `路径概括与拓展/` | 原生路径 → 3 条跨窗口技术路线的概括 |
| `语义相似度匹配/` | 主路径相似专利拓展（bge 预筛 + LLM 精判） |
| `数据集合并/` | 研究数据集 05_合并数据集.csv（2863 专利） |
| `实体抽取，知识图谱构建/` | 知识图谱数据包与构建流程 |
| `专利数据合并与引文网络构建/` | 引文网络构建脚本与节点表 |
| `docs/` | 研究设计文档与思路梳理 |
| `多智能体叙事/` | 多智能体情报分析方案 |

## 复现

```powershell
cd 替代风险计算
python -m pytest tests -v          # 59 个测试全绿
python scripts/prepare.py          # 中间表（大文件依赖见 数据清单.md）
python scripts/embed_similarity.py # 实体嵌入相似度（bge-small-zh-v1.5）
python scripts/run_all.py          # 指标总表
python scripts/generate_report.py  # 报告
```

版本切换：`config.json` 的 `input_suffix` = `""` / `"_filtered"` / `"_paths"` / `"_paths_filtered"`。
LLM 相关性判定需 `DEEPSEEK_API_KEY` 环境变量（不落盘）。

## 方法要点

**R = S × (M + V) ÷ 2**：S=可替代性（功能/场景相似度 + 原理差异，bge 嵌入对称最佳匹配）、M=替代成熟度（增长优势 + 主路径地位转移）、V=安全暴露度（国外核心专利控制度 + 国内自主能力缺口）。国内外=公开国家；软阈值口径；高价值=被引前 10%（44 次）；sigmoid 归一。

大文件（原始 xlsx、GraphML、嵌入缓存等）不随仓库分发，清单与再生方式见 `数据清单.md`。

## 免责

数据与结论仅供研究使用；仓库不含任何 API 密钥。
